import { StyleSheet } from 'react-native';

const white = '#fff';
const gray = 'gray';

export const style = StyleSheet.create({
    submitButton: {
        margin: 10,
        backgroundColor: gray,
        color: white,
    },
});
